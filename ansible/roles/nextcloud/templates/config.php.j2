<?php
$CONFIG = array (
  'instanceid' => '{{ nextcloud_instance_id | default("oc" ~ 999999|random) }}',
  # passwordsalt and secret sign sessions and tokens. They MUST be provided
  # via Ansible Vault (see the fail task in tasks/main.yml). An empty value
  # is a critical weakness, so we never fall back to a default here.
  'passwordsalt' => '{{ nextcloud_password_salt }}',
  'secret' => '{{ nextcloud_secret }}',
  'trusted_domains' =>
  array (
{% for domain in nextcloud_trusted_domains %}
    {{ loop.index0 }} => '{{ domain }}',
{% endfor %}
  ),
  'datadirectory' => '/var/owncloud/data',
  'logfile' => '/var/owncloud/data/nextcloud.log',
  'log_type' => 'file',
  'loglevel' => 2,
  'dbtype' => 'mysql',
  'version' => '{{ nextcloud_version }}',
  'overwrite.cli.url' => 'https://{{ nextcloud_overwrite_host }}',
  # Force the canonical host/protocol so Nextcloud builds correct URLs even
  # when served from a subdirectory behind a proxy.
  'overwritehost' => '{{ nextcloud_overwrite_host }}',
  'overwriteprotocol' => 'https',
  'dbname' => '{{ nextcloud_db_name }}',
  'dbhost' => '{{ nextcloud_db_host }}',
  'dbport' => '',
  'dbtableprefix' => 'oc_',
  'mysql.utf8mb4' => true,
  'dbuser' => '{{ nextcloud_db_user }}',
  'dbpassword' => '{{ nextcloud_db_password }}',
  'installed' => true,

  'mail_smtpsecure' => 'tls',
  'mail_smtphost' => 'smtp.zoho.com',
  'mail_smtpport' => '587',
  # Keep the update checker ON so the admin UI surfaces available security
  # updates. Actual upgrades are driven by Ansible (bump nextcloud_version).
  'updatechecker' => true,
  'mail_from_address' => 'admin',
  'mail_domain' => 'zer0c00l.in',
  'updater.release.channel' => 'stable',
  'mail_sendmailmode' => 'smtp',

  'objectstore' => array(
    'class' => '\\OC\\Files\\ObjectStore\\S3',
    'arguments' => array(
      'bucket' => '{{ nextcloud_s3_bucket }}',
      'autocreate' => {{ nextcloud_s3_autocreate | ternary('true', 'false') }},
      'key' => '{{ nextcloud_s3_access_key }}',
      'secret' => '{{ nextcloud_s3_secret_key }}',
      'region' => '{{ nextcloud_s3_region }}',
      'hostname' => '{{ nextcloud_s3_hostname }}',
      'port' => {{ nextcloud_s3_port }},
      'use_ssl' => {{ nextcloud_s3_use_ssl | ternary('true', 'false') }},
      'use_path_style' => {{ nextcloud_s3_use_path_style | ternary('true', 'false') }},
    ),
  ),
);
